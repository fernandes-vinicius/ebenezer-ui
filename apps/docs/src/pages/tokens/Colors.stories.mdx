import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs";
import { themes, ThemeProvider, convert, ensure } from "@storybook/theming";
import { Box, Flex, Heading, Text } from "@ebenezer-ui/react";
import { colors } from "@ebenezer-ui/tokens";
import { getContrast } from "polished";

<Meta title="Tokens/Colors" />

<Heading size="xl">Colors</Heading>

<Text>
  The Ebenezer UI includes an out-of-the-box default color palette built on top
  of the latest, most modern color accessibility standards.
</Text>

<div>
  {Object.entries(colors).map(([key, color]) => {
    return (
      <Box key={key} css={{ backgroundColor: color, padding: "$8" }}>
        <Flex
          justify="between"
          align="center"
          css={{
            color: getContrast(color, "#FFF") < 3.5 ? "#000" : "#FFF",
          }}
        >
          <Text css={{ fontWeight: "$bold" }}>{`$${key}`}</Text>
          <Text>{color}</Text>
        </Flex>
      </Box>
    );
  })}
</div>
