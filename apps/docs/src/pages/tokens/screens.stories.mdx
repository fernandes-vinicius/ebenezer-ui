import { Meta } from "@storybook/addon-docs";
import { screens } from "@ebenezer-ui/tokens";
import { Title } from "../../components/Title";
import { Description } from "../../components/Description";
import { TokensGrid } from "../../components/TokensGrid";

<Meta title="Tokens/Screens" />

<Title>Screens</Title>

<Description>
  You set your project's <code>`breakpoints`</code> in the{" "}
  <code>`screens`</code> section. The braces become your responsive modifiers
  (like <code>`@md: 'text-center'`</code>) and the values ​​are the minimum
  width where this breakpoint should start.
</Description>

<TokensGrid tokens={screens} />
